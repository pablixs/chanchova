{
  "name": "chanchova",
  "version": "0.0.0",
  "private": true,
  "description": "Chanchova - juego del Chancho argentino multijugador online",
  "scripts": {
    "dev": "turbo run dev",
    "dev:api": "turbo run dev --filter=@chanchova/api",
    "dev:web": "turbo run dev --filter=@chanchova/web",
    "build": "turbo run build",
    "build:api": "turbo run build --filter=@chanchova/api",
    "build:web": "turbo run build --filter=@chanchova/web",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "sim": "pnpm --filter @chanchova/engine sim",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.6.3",
    "rimraf": "^6.0.1"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.12.3"
}
